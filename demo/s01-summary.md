# s01 总结

本章介绍了最基础的智能体循环：把用户请求发给模型，若模型发起工具调用，就执行工具并把结果追加回对话，再继续请求模型；直到 `stop_reason` 不再是 `tool_use` 才结束。

核心思想是：**一个工具加一个循环，就能构成最小可用智能体**。这里的工具是 `bash`，循环负责把模型与真实世界连接起来，让模型能够读文件、执行命令、查看结果并继续推理。

文中还给出了完整实现：维护累积消息列表，持续调用模型，执行每个工具请求，并把工具结果作为新的 user 内容送回。整个实现不到 30 行，但它是后续所有能力扩展的基础。
